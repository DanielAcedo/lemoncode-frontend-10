import { css } from 'emotion';
import { theme } from 'core/theme';

export const labelStyle = css`
  margin-top: ${theme.spacing(2)}px;
`;

export const textRowStyle = css`
  display: flex;
  flex-direction: row;
`;

export const addButtonStyle = css`
  display: flex;
  justify-content: center;
`;
